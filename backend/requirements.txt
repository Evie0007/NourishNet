# Pinned to the versions this app was built and tested against (NFR-4.1.8).
# Unpinned, any build could silently pull a different — or compromised —
# version, and a deploy that worked yesterday breaks today.
#
# To upgrade: bump deliberately, run the app, then commit the new pin.

fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3
python-multipart==0.0.32
email-validator==2.3.0

# Auth
bcrypt==5.0.0
pyjwt==2.13.0

# OCR pipeline. Load-bearing: app/routers/intake.py imports app/ocr.py, which
# imports this inside the function that uses it. It is the single largest thing
# in the build (grpcio, protobuf), but commenting it out now breaks the label
# scanner — to save build weight, unset GOOGLE_APPLICATION_CREDENTIALS instead
# and let label reads come back empty.
google-cloud-vision==3.15.0
