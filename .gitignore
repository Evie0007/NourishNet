__pycache__/
*.pyc
.env
gcp-vision-key.json
.venv/
venv/

# SQLite dev databases, plus the -wal/-shm sidecars that WAL mode creates
# alongside them (see the pragmas in app/database.py).
*.db
*.db-wal
*.db-shm
